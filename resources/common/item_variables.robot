*** Settings ***
Documentation     initialize item variables to be used in shop and cart test cases

*** Variables ***
${overall_total_qty}    0
${overall_total_amount}    0
${name_index}     0
${price_index}    1
${quantity_index}    2
${subtotal_index}    3
